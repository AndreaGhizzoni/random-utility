package randutil

import "testing"

func TestGetInt(t *testing.T) {
	/*
		m := 1
		tm := reflect.TypeOf(m)
		M := 10
		tM := reflect.TypeOf(M)
		r := Get(m, M)
		tr := reflect.TypeOf(r)
		if tr != tm || tr != tM {
			t.Errorf("Get() return %v that is != from %v", tr, tm)
		}
	*/
}
